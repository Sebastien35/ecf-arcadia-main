<form method="post" action="{{ path('app_sevice_delete', {'id': service.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="btn">Supprimer</button>
</form>
